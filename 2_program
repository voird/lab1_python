def count_russian_chars(string):
    counter = 0
    for char in string:
        if 'а' <= char <= 'я' or 'А' <= char <= 'Я':
            counter += 1
    return counter

def is_palindrome(s):
    s_lower = s.lower()
    return s_lower == s_lower[::-1]
